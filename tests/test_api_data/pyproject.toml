[project]
name = "test-api-data"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pytest>=9.0.2",
    "requests==2.31.0",
    "pandas>=2.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = ["-v", "--strict-markers"]